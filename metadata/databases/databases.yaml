- name: pizzastack
  kind: postgres
  configuration:
    connection_info:
      database_url: postgres://postgres:postgrespassword@postgres:5432/postgres
      isolation_level: read-committed
      use_prepared_statements: false
  tables: "!include pizzastack/tables/tables.yaml"
